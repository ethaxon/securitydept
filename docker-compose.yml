name: securitydept

services:
  securitydept-server:
    # build: .
    image: ghcr.io/ethaxon/securitydept:latest
    ports:
      - 7021:7021
    environment:
      - SECURITYDEPT_CONFIG=/app/config.toml
    volumes:
      - ./config.toml:/app/config.toml
      - ./claims-script-check.mts:/app/claims-script-check.mts
      - ./data:/app/data
      # - ./webui:/app/webui # for custom webui
